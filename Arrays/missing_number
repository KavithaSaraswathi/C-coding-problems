#include <stdio.h>

int main(){
    int i,n,total,sum=0;
    int arr[100];
    printf("Enter number of elements:");
    scanf("%d",&n);
    
    for(i=0;i<n;i++){
        printf("Enter the value %d:",i+1);
        scanf("%d",&arr[i]);
    }

    for(i=0;i<n;i++){
        sum+=arr[i];
   }
    
    total=n*(n+1)/2;
    printf("Missing number is %d",sum-total);
    return 0;
} 
