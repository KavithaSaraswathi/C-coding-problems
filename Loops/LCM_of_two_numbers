#include <stdio.h>

int main() {
    int a, b, x, y, temp;

    scanf("%d %d", &a, &b);

    x = a; y = b;

    while (b != 0) {
        temp = a % b;
        a = b;
        b = temp;
    }

    printf("LCM = %d", (x * y) / a);
    return 0;
}
