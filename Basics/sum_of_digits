#include <stdio.h>

int main() {
    int a, b, c, sum = 0;

    printf("Enter a number: ");
    scanf("%d", &a);

    b = a;

    while (b != 0) {
        c = b % 10;
        sum += c;
        b = b / 10;
    }

    printf("Sum = %d", sum);

    return 0;
}
