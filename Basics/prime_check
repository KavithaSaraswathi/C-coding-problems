#include <stdio.h>

int main() {
    int a, flag = 1;

    scanf("%d", &a);

    for(int i = 2; i < a; i++) {
        if (a % i == 0) {
            flag = 0;
            break;
        }
    }

    if (flag == 1)
        printf("Prime");
    else
        printf("Not Prime");

    return 0;
}
